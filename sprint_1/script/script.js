let pcvalue = 820000
let form = document.getElementById('form')

form.addEventListener('submit', (e) => {
    e.preventDefault();
    
})

function numTotal() {
    let npc = Number(document.getElementById('pcnumber'))
    let numPc = ["", "true"]
    numPc[0] = npc
    if (numpc = true) {
        let test = 123
        return test
    }
    console.log(pcvalue, numPc, test)
}